# SQLite Backend Tests

This directory is reserved for future SQLite backend tests.

Tests that require SQLite-specific dependencies should be gated so `make check` remains
usable for core development without optional backend dependencies.
