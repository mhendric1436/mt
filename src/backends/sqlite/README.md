# SQLite Backend

This directory is reserved for the future optional SQLite backend implementation.

The public dependency-free skeleton currently lives at:

```text
include/mt/backends/sqlite.hpp
```

Do not add a SQLite client dependency to the core library build.
